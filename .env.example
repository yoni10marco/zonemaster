# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Gemini API key from Google AI Studio (server-only). Enables the AI coach.
GEMINI_API_KEY=
# Optional: override the coach model (default gemini-3.5-flash, falling back to
# gemini-3-flash-preview when it is overloaded).
GEMINI_MODEL=
